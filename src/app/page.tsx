import { ModeToggle } from "@/components/mode-toggle-theme";
import { Button } from "@/components/ui/button";

const Home = () => {
  return ( 
    <main>
      <Button >Click</Button>
      <button>CLick</button>
      <ModeToggle/>
    </main>
   );
}
 
export default Home;