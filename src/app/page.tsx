import Header from "@/components/header";

const Home = () => {
  return (
    <main id="main" className="flex justify-center w-full h-screen ">
      <Header/>
    </main>
  );
};

export default Home;
